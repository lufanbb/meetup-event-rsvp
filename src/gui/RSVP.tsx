import React from "react";

interface Props{
    accessToken: string;
    eventId: string;
}

interface RSVP {

}

const RSVPURL = ``;

export const EventRSVP :React.FunctionComponent<Props> = (props: Props) => {
    return (
        <div>

        </div>
    )
}